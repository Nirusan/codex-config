interface:
  display_name: "Full Validation"
  short_description: "Run the repo's full validation suite"
  default_prompt: "Use $validate to run the full validation suite for this repo."

policy:
  allow_implicit_invocation: true
